#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

char alphabet[24] = "abcdefghijklmnopqrstuvwxyz";
for (int i = 0 ; i <= 24 ; i++)
{
putchar(alphabet[i]);
}
return (0);
}
