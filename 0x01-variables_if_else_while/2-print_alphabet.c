#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

char alphabet[25] = "abcdefghijklmnopqrstuvwxyz";
for (int i = 0 ; i <= 25 ; i++)
{
putchar(alphabet[i]);
}
return (0);
}
